h2 Users List :
a(href='/users/create') New
form(action='/users/search', method='Get')
	input(type= 'text', name= 'q', value= paramValue)
	button Search
ul 
	each user in users
		li
			span= user.name
				span &nbsp;
				a(href='/users/' + user.id) Views